Run this project by using the `make` command, or the following:

```
docker build --platform=linux/amd64 -t upx-golang-m1-test:latest .
docker run --platform=linux/amd64 -t upx-golang-m1-test:latest
```
